# Pattystack Remotion terminal demo

A code-defined terminal animation showing @puffle/pattystack fake subs, routing, a curl request, and token usage.

## Render

```bash
npm install
npm run render:mp4   # out/TerminalDemo.mp4
npm run render:gif   # out/TerminalDemo.gif
```
